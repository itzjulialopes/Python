print(" Type an integer: ")
number = int(input())
print(number - 1, number + 1 )

#or, in a more sophisticated way

n = int(input("type an integer: "))
a = n + 1
b = n - 1 
print( a, b)

# in an even more sophisticated way

n = int(input("type an integer: "))
a = n + 1
b = n - 1 
print("the predecessor is {}, and the successor is {}".format(n,a,b))  
